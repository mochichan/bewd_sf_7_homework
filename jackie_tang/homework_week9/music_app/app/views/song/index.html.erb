<h2>Song List:</h2>

<ul>
	<% @songs.each do |song|%>
	<li><%= link_to song.title, song_path(song.id) %></li>
	<% end %>
</ul>

<%= link_to "Add Song", new_song_path %> 

<%= link_to "Back", root_path%>